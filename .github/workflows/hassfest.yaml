name: Validate with hassfest (disabled)

# This workflow previously ran Home Assistant's hassfest action.
# The project is no longer linked to Home Assistant; disable triggers.
on: []

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "hassfest disabled for this repository"
