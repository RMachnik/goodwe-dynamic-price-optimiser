version: "2"
tunnels:
  goodwe-web-ui:
    proto: http
    addr: 8080
    schemes: [https]
    # IMPORTANT: Security layer - CHANGE THESE CREDENTIALS
    basic_auth: ["admin:mnikow236"]
    domain: unreticent-unpantheistically-kathryn.ngrok-free.dev
    inspect: true
    host_header: localhost:8080
