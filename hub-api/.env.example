# Production Environment Configuration Template
# This file shows the structure - use manage-secrets.sh to set values securely

# Database (PostgreSQL from Mikrus)
DATABASE_URL=postgresql+asyncpg://andrzej358:PASSWORD@psql01.mikr.us:5432/db_andrzej358

# JWT Security (generate with: openssl rand -hex 32)
JWT_SECRET_KEY=CHANGE_ME_TO_RANDOM_STRING

# MQTT Broker (RabbitMQ from Mikrus)
MQTT_BROKER=mws03-52071.wykr.es
MQTT_PORT=1883
MQTT_USER=hub_api
MQTT_PASS=CHANGE_ME

# CORS Origins (no domain, using direct VPS access)
CORS_ORIGINS=http://localhost:5173,http://srv26.mikr.us:40314,http://srv26.mikr.us

# Admin User (created on first startup)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Note: Use ./scripts/manage-secrets.sh vps to set these securely
